<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobiledoctor.parvez.com.mobiledoctor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".userClass.UserResgistationActivity" />
        <activity android:name=".DoctorClass.ListViewActivity" />
        <activity android:name=".DoctorClass.AddDoctorActivity" />
        <activity android:name=".MedisineClass.MadicineDocumentActivaty" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MedisineClass.MedisinlistviewActivity" />
        <activity android:name=".MedisineClass.MedicinViewActivity" />
        <activity android:name=".DoctorClass.EditDoctorActivity" />
        <activity android:name=".DoctorClass.DoctorProfileActivity"></activity>
    </application>

</manifest>